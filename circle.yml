#
# Build configuration for Circle CI
#
machine:
    environment:
        ANDROID_HOME: /usr/local/android-sdk-linux

dependencies:
    override:
        - echo y | android update sdk --no-ui --all --filter tools,platform-tools,build-tools-23.0.3,android-23,extra-google-m2repository,extra-google-google_play_services,extra-android-support
        - ANDROID_HOME=/usr/local/android-sdk-linux ./gradlew dependencies

test:
    pre:
        - emulator -avd circleci-android23 -no-audio -no-window:
            background: true
            parallel: true
        - circle-android wait-for-boot
    override:
        - ./gradlew testDebug connectedDebugAndroidTest --console=plain --stacktrace --info
        - cp -r sample/build/outputs/androidTest-results/* $CIRCLE_TEST_REPORTS
        - cp -r requestmatcher/build/test-results/debug/* $CIRCLE_TEST_REPORTS
        - cp -r requestmatcher/build/outputs $CIRCLE_ARTIFACTS